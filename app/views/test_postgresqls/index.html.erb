<%- model_class = TestPostgresql -%>
<div class="pagination-centered">
	 <%= will_paginate @test_postgresqls%>
</div>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:content) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @test_postgresqls.each do |test_postgresql| %>
      <tr>
        <td><%= link_to test_postgresql.id, test_postgresql_path(test_postgresql) %></td>
        <td><%= test_postgresql.content %></td>
        <td><%=l test_postgresql.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_test_postgresql_path(test_postgresql), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      test_postgresql_path(test_postgresql),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_test_postgresql_path,
            :class => 'btn btn-primary' %>

<%= link_to "Insert into postgresql",
            populate_test_postgresqls_path,
            :class => 'btn btn-primary' %>


